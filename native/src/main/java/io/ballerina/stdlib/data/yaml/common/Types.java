package io.ballerina.stdlib.data.yaml.common;

public class Types {

    public enum Collection {
        STREAM,
        SEQUENCE,
        MAPPING
    }

    public enum FailSafeSchema {
        MAPPING,
        SEQUENCE,
        STRING
    }
}
